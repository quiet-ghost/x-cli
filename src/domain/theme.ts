export type AppTheme = {
  id: string
  label: string
  colors: {
    background: string
    panel: string
    panelMuted: string
    surface: string
    surfaceActive: string
    border: string
    text: string
    textMuted: string
    primary: string
    accent: string
    success: string
    warning: string
    error: string
    overlay: string
  }
}
